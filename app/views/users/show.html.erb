<!--ユーザーの詳細情報-->
<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100, 100) %>
  <% if @user == current_user %>
  <!--ここで引数に指定している(@user)はshowアクションの特定ユーザー-->
    <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
  <% end %>
</div>

<!--上記ユーザーが投稿した画像一覧-->
<!--部分テンプレート呼び出し記述-->
<%= render 'app/views/postimages/list', postimages: @postimages %>